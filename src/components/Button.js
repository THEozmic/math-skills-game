import React, { Component } from "react";

class Button extends Component {
  render() {
    return (
      <div className="item">
        <button>=</button>
      </div>
    );
  }
}

export default Button;
